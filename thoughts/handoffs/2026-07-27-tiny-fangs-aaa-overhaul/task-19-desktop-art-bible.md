---
date: 2026-07-27T18:11:56-04:00
task_number: 19
status: accepted-by-user-manual-pass
scope: desktop-only
---

# Task 19 Handoff: Reference-Locked Desktop Art Bible

## Outcome

Created the measurement/specification artifact:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md`

It converts both immutable 1672 × 941 references into explicit desktop composition, color,
lighting, card, material, typography, identity, rejection, fixture, crop, and critic targets. It
does not modify production visuals or claim independent acceptance.

## Independent Critic Rejections and Corrections

A fresh critic rejected first revision
`efed9c0aa0c37017b67b1f4bf4894abd82a7482d9edbe9bad53506e666419746` at 84.2/100 and
wow 7.8/10, with five P1 and two P2 findings. The first hash is superseded.

The correction pass applied every bounded finding:

1. Replaced the camera-invalid projected-card ratio gate with a card-local unprojected chassis
   ratio and winning-camera per-anchor four-corner golden quadrilaterals.
2. Added exact seven-category weights, total formula, 9.0 anchors, wow scale, P0–P3 definitions,
   and Boolean pass math.
3. Corrected hand center/envelope/four-card centers; moved both right rails to a 24 px safe inset;
   defined 24/12 px separation gates; and specified an exact 2 × 3 action/focus grid.
4. Made physical-edge-to-aperture inset cumulative at 10.5–12.5% per side and contained both
   bevels inside the family rail.
5. Added fixed title/nameplate geometry, 21 exact crop rectangles, and a versioned deterministic
   populated-fixture/hash contract with exact cards, HUD, statuses, actions, Set privacy, and log.
6. Corrected the lower-meadow median luminance relationship to 7.6%, target 7–10%.
7. Corrected sampled navy/parchment contrast to 7.01:1.

The camera remains deliberately unlocked.

A different fresh critic then rejected second revision
`caf117e208d32a89366f9299bcb946da8cb9579dde52a5adf26f39da257a0742` at 86.85/100 and
wow 8.1/10, with three P1 and two P2 findings.

The second correction pass:

1. Split the one-revision camera-graybox gate from the seven-category final-art gate. Camera
   scoring now uses camera-specific weights, anchors, math, unanimity, geometry/determinism, and
   zero P0/P1 without a wow or two-revision requirement.
2. Added a versioned four-card showcase using original `duskfang`, `manaSurge`, `phantomWall`, and
   `soulTrap` presentation identities, with exact ordered corners, centers, rotations, z-order,
   shadows, crop containment, and canonical hash procedure.
3. Added exact 333 × 505 creature/Cast/Set/back safe rectangles, family deltas, non-overlap,
   250 × 60 title geometry, fixed two-line typography, and longest-name fixtures.
4. Measured the player-active/hand envelope intersection as 80 × 8 px and added a deterministic
   ≥4 px visible/semantic separation gate with fixed correction precedence and no implicit anchor
   movement.
5. Added signed creator and independent-reviewer motif/provenance attestations tied to exact asset
   manifest rights/provenance hashes.

All prior corrections remain in force and O/P remains unlocked.

A third fresh critic rejected revision
`762086d3e8afe641c2e16bf6346d65e41f73fef2115c0eb5d3063abe898b9571` despite a
93.3/100 weighted total: wow was 8.9/10 and the review contained three P1 and one P2 finding.

The third bounded correction pass:

1. Added versioned `desktop-integrated-overlay` and `desktop-static-fallback` fixtures with exact
   states, privacy transitions, action outcomes, focus behavior, crops, canonical hashes, and an
   end-to-end static-mode playability assertion. Wired both fixtures into the S/A anchors and
   integrated packet rather than applying final-art requirements to camera grayboxes.
2. Added `tiny-fangs.performance-report.v1` with named hardware/browser/quality metadata,
   10-second warm-up, three 30-second settled traces, p50/p95/max reporting, desktop-high budgets,
   renderer counts, memory reporting, trace/report hashes, and explicit pass/fail reasons.
3. Replaced the card-local inspect glyph as a semantic control with the whole visible
   board-scale card silhouette or a fixed ≥44 × 44 CSS hit polygon. Added focus-ring,
   collision/occlusion, keyboard-return, and opponent-Set privacy evidence to the golden packet.
4. Moved the opponent HUD center to `(836,52)`, recomputed normalized y to `0.0553`, and locked
   bounds `(576,24,1096,80)` so all semantic paint begins at y ≥24. Explicitly classified both
   immutable PNGs as untagged RGB, assigned IEC 61966-2-1 sRGB before every metric, and required
   production/reference captures to embed or manifest-declare sRGB.

All earlier camera, composition, card, scoring, showcase, and provenance gates remain in force.
O/P remains unlocked.

A fourth fresh critic rejected revision
`9c3e2ecdeecef1c13a629893d867debce4b59318a464c75e0a6436443ae24c2e` at 91.2/100 and
wow 8.8/10, with three P1 and two P2 findings.

The fourth bounded correction pass:

1. Rewrote §13.9 as revision 2 with named hardware tiers. `desktop-minimum` is the acceptance
   gate and must be named with the same rigor the plan demands of `mobile-reference`;
   `desktop-reference` records headroom only; `desktop-throttled-ci` detects regressions and can
   never grant a pass. A reference-only run is EVIDENCE PENDING, not a pass.
2. Gave every previously method-free budget an exact instrument, window, and cache state:
   interaction long tasks use `PerformanceObserver` longtask/event-timing entries over a named
   eleven-interaction set driven through real input paths; the three payload budgets use Resource
   Timing `encodedBodySize` over exactly defined windows, with **first playable** given a
   two-condition definition the runner records.
3. Added §3.1.1: six immutable R2-traced quadrilateral targets with derived
   angle/ratio/convergence/foreshortening intervals and propagated per-coordinate uncertainty.
   Camera candidates are scored by residuals against these independent traces; candidate-generated
   quads are downstream registration truth only and may not score themselves. This removes the
   circular candidate-generated fidelity geometry.
4. Added §13.1.1's exact pre/reveal/post contract: the `authorized-set-reveal` variant is a
   three-event ordered timeline (`event0-hidden-pre`, `event1-overlay-reveal`,
   `event2-public-post`) with per-event authoritative/public/presentation/accessibility/privacy
   hashes, exact allowlisted deltas, and zero-match identity scans before reveal. This resolves
   the contradictory frozen-state-versus-reveal contract.
5. Added §13.7.1–§13.7.2: a canonical machine-checkable packet index
   (`tiny-fangs.critic-packet-index.v1`) whose required-artifact set derives from the packet kind,
   and one single hash-omission rule that names each schema's own-digest field and forbids hashing
   around a pending nested digest.
6. Added §13.8.1–§13.8.2: a committed trusted-signer registry
   (`tiny-fangs.signer-registry.v1`) with roles, validity intervals, and append-only status
   transitions, plus verification-time validity rules distinguishing retirement from revocation
   and requiring re-review/re-signing of records relying on a revoked key.

All prior corrections remain in force and O/P remains unlocked.

A fifth fresh critic rejected revision
`8d5f84d03cf9380957d26b3bbe98137643946036a8dc0d02b091c392013d3955` at 88.3/100 and
wow 8.5/10, with three P1, six P2, and ten P3 findings. Notably, the critic verified every
recomputable number it attacked — all 13 palette samples, 15 luminance quantiles, all 36 §3.1.1
derived values, all 68 normalized coordinates, showcase corner reproduction to 0.049 px, and
engine-true fixture semantics — and the rejection rested entirely on protocol-layer defects.

The fifth bounded correction pass applied every P1/P2 and all ten P3s:

1. **Sealed blind mappings (P1).** The §13.7.1 packet index had forced critics to byte-verify
   every packet file including the O/P and A/B mappings, defeating the blind by construction.
   Both mappings are now external files represented in-packet only by SHA-256 commitments with a
   dedicated `sealed-external` index status; the coordinator reveals the file after score
   submission and it must hash to the commitment. A physically present mapping file is itself the
   P0 mapping leak.
2. **Measurable shadow/key/quiet-zone methods plus a stack allowance (P1).** §6.1 now requires
   renderer-exported cast-shadow/footprint/ID masks and defines contact centroid, PCA key
   direction, and quiet-zone classification from them (manual traced masks, I-labeled, ±8 px for
   R2). A new stack/deck/showcase tolerance row (centroid x +20–100, y +50–90 px) covers R2's
   measured stack shadows (+25…+95 / +60…+80 px), so a faithful challenger can no longer fail
   the single-card row for reproducing the reference.
3. **Pinned performance quality tier (P1).** The §13.1 frame block now states the exact frozen
   values (quality tier `desktop-high`, ambient time t = 0, seed `414243`, seeded zero-emission
   particle state) and §13.9.4 gained the explicit `desktop-high` conjunct; the §12 row was
   relabeled to §13.9 rev 2 authority.
4. **One rulebook per gate (P2).** §13.5 states its weight-vector supersession of the plan's
   proportional N/A redistribution for the desktop stills packet, §13.9.1 states its
   supersession of the plan's desktop pass authority/fixture, and the plan carries both matching
   amendments. §13.5 also restates the plan's ≥75% crop-win floor so the bible alone runs the
   full gate.
5. **Divider re-measured under a stated definition (P2).** The luminous band is now
   threshold-defined (prominence >0.10, occlusion gaps ≤100 px bridged): x ≈ 169–1473, span
   1304 px (78.0%), center x ≈ 821, ends ±8 px, with the occluded x ≈ 370–455 stretch and
   same-hue continuity documented. Acceptance band widened to 68–80%; the 1168 px/±3 px claim is
   superseded. The diamond is threshold-defined (core ≈ 69 × 44 px at lum >0.55 excluding line
   rows; glow ≈ 93 px at >0.35) with tolerance on the core only; divider clearance gaps restate
   as 44/35 px with their exact derivation.
6. **Executable predicates and hash unification (P2).** `cameraGeometryDeterminismPass` and
   `mandatoryMeasurementsPass` are now enumerated conjunctions of named §12 rows; §13.1's hash
   requirement defers to the single §13.7.2 own-digest rule; the §13.1.1 field is
   `publicStateSha256` everywhere.
7. **P3 batch.** Rotated-utility landmark widened to ±8 px; §7.4 nested-pair non-overlap
   exemption and the per-axis 333 × 505 → render-size rescale rule; §12 gained the Cast/Set
   CIEDE2000 ≥12 row; §8.2's board-active name minimum must now be proven at the board-active
   nameplate scale; §13.3 gained the declared rule-driven divergence list; §13.9.4 replaced
   "no per-frame DOM layout reads" with a stack-attributed forced-reflow trace instrument.

All prior corrections remain in force and O/P remains unlocked.

A sixth fresh critic rejected revision
`fe3d3b255c5268427b9edb93239610958e4074a5e2d83ae7953e1a99b70476b8` at 89.1/100 and
wow 8.5/10, with one P1, eight P2, and six P3 findings. It again verified every recomputable
number (all palette samples, quantiles, §3.1.1 derivations, showcase corner reproduction at
0.00 px, action-grid/rail arithmetic, catalog cross-checks); the sole P1 was that the fifth
pass's divider/diamond prominence definitions, executed literally, qualified edge flowers/rocks
and sunlit grass (band 99.6% instead of 78%) and could not reach the diamond's true width.

The sixth bounded correction pass applied all fifteen findings:

1. **Executable divider/diamond metrology (P1).** The band now requires peak-row consistency
   (409–419), run participation ≥16 px, a central-third seed, and ≤130 px occlusion bridging;
   executed on R2 it yields x ≈ 173–1455, span 1283 px (76.7%), center x ≈ 814, with no
   participating run excluded. The diamond is defined by a vertical-thickness profile
   (extent of lum >0.55 in rows 395–439 ≥ line baseline + 4 px), yielding a 39 × 44 px core at
   x 818–856; §4.1 locks re-derived to 68–80% span and 33–45 × 38–50 px core. The unmeasurable
   0.35 glow threshold was withdrawn with its reason stated.
2. **Envelope-versus-face measurement classes (P2).** §2.1 now separates §3.1.1 crisp-face quad
   authority from painted-envelope rows (stack thickness/shadow included, ±8 px), resolving the
   opponent active/center-small contradictions and grounding the clearance derivation.
3. **Index completeness and Q-anchor deadlock (P2).** The §13.7.1 enumerations now name every
   §13.7 prose artifact for both packet kinds with a same-set-by-definition P0 rule; the §13.4 Q
   anchor verifies the sealed commitment row, never the mapping file.
4. **Gate enumeration and divergence-list fixes (P2).** `mandatoryMeasurementsPass` gained the
   active/hand gap and equal-card depth rows; the §13.3 divergence list now states the player
   grave replaces R2's rotated leaf utility card while the opponent grave replaces grass.
5. **Third plan amendment (P2).** The plan now adopts §13.4 as the sole camera gate and declares
   `board-midgame-reference` to be `desktop-reference-populated` under the winning camera.
6. **Interface material grammar (P2).** New §9.5 documents the rail/panel ground, the exact
   six-token inventory with §7.2-derived construction, constructed action glyphs, the 150 × 227
   nominal hand card, and log material — the artifact the §13.6 S gate scores against.
7. **Method bindings and remaining fixes (P2/P3).** §12 gained a preamble binding
   environment-frame occupancy, saturation, haze, grass micro-detail, and highlight-perimeter to
   mask formulas; palette-row final scope stated; single-card shadow band relabeled
   authored-from-key-light; visible-back-pixels wording; §3.1 span updated; diamond-center
   wording fixed to the line axis; fixture registry must pin full deck/hand lists and opponent
   max mana corrected to 3.

All prior corrections remain in force and O/P remains unlocked.

## Reference Inspection and Measurement Method

- Inspected both supplied references at original resolution, not a thumbnail:
  - `image-1.png`: card/material close-up;
  - `image-2.png`: field/camera/composition.
- Used top-left integer pixel coordinates.
- Verified both PNGs decode as untagged `RGB` with no ICC/PNG color-space declaration; assigned
  IEC 61966-2-1 sRGB explicitly with Pillow 11.3.0 before point, median, CIEDE2000, or luminance
  work.
- Used per-channel medians and WCAG relative-luminance quantiles for defined source rectangles.
- Traced card, slot, divider, hand, and utility bounding boxes to visible edges.
- Separated direct measurements (`M`) from locked production constraints (`L`), bake-off
  decisions (`B`), and still-image interpretations (`I`).
- Recorded ±3 px trace uncertainty for crisp card/slot edges and ±8 px for painted glow,
  foliage, and shadow envelopes.

## Specification Coverage

The bible includes:

- 13 measured R2 board landmarks;
- a 17-surface Tiny Fangs anchor map for both decks, both active cards, four bench positions,
  both Sets, both graves, hand fan, player/opponent HUD, action rail, and log rail;
- normalized and pixel centers/screen envelopes at 1672 × 941;
- safe areas, center quiet zone, divider, and crop rules;
- calibrated orthographic and low-FOV perspective hypotheses;
- measurable blind camera-bake-off capture and scoring requirements;
- authored depth layers and river/fence/tree/rock/flower placement envelopes;
- 13 exact/median source palette roles with source coordinates, sRGB, hex, and contrast use;
- source-region luminance ranges;
- light direction, contact-shadow geometry, atmosphere, saturation, and focus limits;
- R1 card bounding boxes, card-local nominal 0.660 chassis with 0.645–0.670 authored range,
  cumulative frame hierarchy, fixed nameplate, panel anatomy, grain/wear/specular/foil limits,
  and card-back constraints;
- typography behavior plus canonical desktop minimum sizes without assuming an unlicensed font;
- creature, Cast Verse, Set Verse, card-back, and five pack-theme distinctions;
- an explicit originality boundary preventing literal reuse of reference motifs;
- locked-now versus camera-dependent properties;
- do/do-not rules, eight automatic rejection examples, and objective tolerance rows;
- a deterministic fixture manifest and hash protocol with no invented completed hash;
- exact field, card, title, contact, and action-state crop rectangles;
- weighted critic math, 9.0 category anchors, wow anchors, P0–P3 definitions, and packet-invalid
  conditions.
- a separate camera-only score/pass formula and integrated-final-art score/pass formula;
- a four-card showcase geometry/hash manifest;
- signed asset-level motif/provenance rights evidence;
- deterministic integrated-overlay and no-WebGL fallback evidence with exact crops/interactions;
- board-scale inspect geometry, focus, collision, and privacy evidence;
- a settled-frame desktop-high performance report schema and pass budgets;
- explicit untagged-reference decode assignment and capture-profile requirements.

## Locked Decisions

- Canonical critic frame is 1672 × 941 CSS px at DPR 1 in sRGB.
- Two-dimensional anchor centers/screen envelopes are fixed composition targets.
- Opponent HUD center/bounds are `(836,52)` / `(576,24,1096,80)`; semantic UI begins at y ≥24.
- The outer 10–15% environment frame, central quiet zone, and divider position are fixed.
- Sampled palette, contrast, luminance envelope, upper-left key, and down-right shadow family are
  fixed calibration targets.
- Card-local unprojected ratio is nominally 0.660, acceptable 0.645–0.670. Projected cards are
  judged by winning-camera golden quadrilaterals at ≤2 CSS px per ordered corner.
- Typography behavior and desktop minimum sizes are fixed.
- The whole visible board-scale card or an independent fixed ≥44 × 44 CSS polygon is the inspect
  target; the card-local glyph is visual only.
- Reference metrics assign IEC 61966-2-1 sRGB before decoding comparisons, and captures embed or
  manifest-declare the same profile.
- Tiny Fangs uses original creatures, symbols, filigree, props, and illustrations while matching
  the references' material and staging language.
- Mobile/tablet/portrait adaptation remains deferred and is not derived from this map.

## Unresolved Camera Decisions

The document intentionally does not select a projection. The upcoming blind bake-off must decide:

- orthographic versus low-FOV perspective;
- exact pitch, FOV/frustum, camera height/distance, aim point, and world scale;
- final equal-object near/far scale ratio;
- camera-induced per-anchor card/slot corner geometry;
- versioned per-anchor four-corner golden quadrilaterals;
- prop parallax and occlusion;
- edge-only foreground softness/DOF;
- final exposure after the populated, lit scene exists.

Candidate O targets a 1.00 ±0.01 equal-object scale ratio. Candidate P tests 22°, 26°, and 30°
vertical FOV with a 1.05–1.12 near/far ratio. Both share the same fixture, screen anchors, lighting,
color pipeline, and capture packet. No implementer may lock the camera without the blinded,
two-critic decision record.

## Files

Added:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md`
- `thoughts/handoffs/2026-07-27-tiny-fangs-aaa-overhaul/task-19-desktop-art-bible.md`

Not modified:

- production source;
- tests;
- package/config/workflow files;
- protected reference images;
- `thoughts/shared/plans/PLAN-tiny-fangs-aaa-presentation.md`;
- `thoughts/shared/GOALS-tiny-fangs-aaa.md`.

## Evidence

Reference and protected-copy SHA-256:

- supplied R1 and repository card copy:
  `4ecb53d517d40edc5a7b4907e6991c6b8dcce40cc34e6a21c14b709ed2228056`;
- supplied R2 and repository board copy:
  `5229e89ad2888ef9f3245d6ecd77605ec56a3cc29a6e378c4c7474659e4b79e7`.

Protected prototype Git blob identities:

- `src/render.js`: `5ace20ad5155e96de3ede0e161cac7d90e7698be`;
- `src/styles.css`: `f8b4c7f99cfb5392ba33d82c9b8bf63bf1d0f9ba`.

Superseded artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `efed9c0aa0c37017b67b1f4bf4894abd82a7482d9edbe9bad53506e666419746`;
- 636 lines, 6322 words.

Corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `caf117e208d32a89366f9299bcb946da8cb9579dde52a5adf26f39da257a0742`;
- 863 lines, 8836 words.

Second-corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `762086d3e8afe641c2e16bf6346d65e41f73fef2115c0eb5d3063abe898b9571`;
- 1100 lines, 10889 words.

Third-corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `9c3e2ecdeecef1c13a629893d867debce4b59318a464c75e0a6436443ae24c2e`;
- 1269 lines, 12736 words, 82348 bytes.

Fourth-corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `8d5f84d03cf9380957d26b3bbe98137643946036a8dc0d02b091c392013d3955`;
- 1492 lines, 15096 words, 98380 bytes.

Fifth-corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `fe3d3b255c5268427b9edb93239610958e4074a5e2d83ae7953e1a99b70476b8`;
- 1616 lines, 16750 words, 109207 bytes.

Sixth-corrected artifact identity:

- `thoughts/shared/TINY-FANGS-DESKTOP-ART-BIBLE.md` SHA-256:
  `8495bae72ae38382207599f532e524bdf2af2defb4b5b677f0db7e58eba2305c`;
- 1699 lines, 17777 words, 116024 bytes;
- the plan gained a third dated amendment (camera-gate authority and fixture naming);
- both new divider/diamond methods were executed against the immutable R2 pixels before their
  results were written in (band 173–1455/76.7%/814; diamond 39 × 44 at x 818–856);
- scripted cross-reference check: zero missing §-references; superseded values absent;
  `git diff --check` clean.
- The plan carries two matching amendments (stills weight vector; desktop performance authority),
  so its content also changed in this pass.

Fifth-correction hygiene:

- every `§n` cross-reference resolves (scripted check, zero missing);
- superseded terms are absent: `publicProjectedStateSha256`, `desktop-high-settled`, the
  1168 × 8 divider span, the 39 × 39 diamond, both "hidden … mapping" phrasings, and the stale
  46/39 px gap claims;
- divider band, diamond core/glow, and occlusion measurements re-derived from the immutable R2
  pixels with the stated prominence thresholds before being written into §2.1/§4.1;
- `git diff --check` clean; final newline present.

Fourth-correction hygiene:

- every `§n` cross-reference resolves to a defined section header (verified by script; zero
  missing references across 64 defined sections);
- camera-graybox weights (40+25+15+15+5) and final-art weights (20+15+20+15+10+10+10) each still
  sum to 100;
- `git diff --check` clean; no trailing whitespace; final newline present;
- the superseded `desktop-high-settled` report ID is absent; §13.9 is `desktop-settled` revision 2;
- all six PENDING sentinels (`PENDING_CAPTURE_RUNNER`, `PENDING_PROVENANCE`,
  `PENDING_PERFORMANCE`) remain invalid-by-definition; no invented completion hash exists;
- both reference hashes and both protected prototype blob hashes re-verified unchanged this
  session.

Hygiene:

- correction audit: passed;
- 14/14 source-palette assertions: retained and passing;
- all normalized hand/rail values and action/crop rectangles: arithmetic-checked;
- category weights sum to 100 and a perfect-score formula evaluates to 100;
- sampled navy/parchment contrast recomputes to 7.0130:1;
- lower/upper R2 Y50 delta recomputes to 7.579%;
- obsolete camera-ratio wording: absent from the corrected specification;
- camera and final-art weight sets independently sum to 100 and evaluate to 100 at perfect scores;
- all four showcase quadrilaterals reproduce from center/size/rotation to 0.1 px;
- all four showcase shadow envelopes have ≥8 px crop clearance;
- creature/Cast/Set safe rectangles are pairwise non-overlapping;
- title geometry is 250 × 60 with a 230 × 50 two-line text box at fixed 24.5 px line height;
- active/hand intersection recomputes to `(528,683,608,691)`, exactly 80 × 8 px;
- HUD y normalization recomputes to 0.0553 and `(576,24,1096,80)` from center/size;
- integrated overlay fixture has four exact mutually exclusive variants and named crops;
- static fallback has six exact reset/dispatch cases, zero-WebGL proof, and a full journey gate;
- board-scale inspect bounds are ≥44 × 44 CSS px with ≥4 px semantic separation;
- both immutable PNGs report `RGB` and empty Pillow info dictionaries; explicit IEC 61966-2-1
  assignment/capture declarations are required;
- performance schema records three 30-second traces after 10-second warm-up, p50/p95/max,
  renderer counts, memory, and threshold reasons;
- §13.1–§13.9 cross-references resolve;
- canonical frame, plan cross-reference, ledger cross-reference, and final newline: present;
- no unintended trailing whitespace;
- repository `git diff --check`: passed with concurrent lanes present;
- no test/build run was required for a documentation-only artifact.

## Acceptance Boundary

A seventh fresh critic rejected sixth-corrected revision
`8495bae72ae38382207599f532e524bdf2af2defb4b5b677f0db7e58eba2305c` at 88.2/100 and wow 8.5/10,
with two P1, six P2, and seven P3 findings. It reproduced the new divider-band method to the
pixel and re-verified the entire empirical core, then found, among others: the canonical §13.1
fixture is unreachable by legal play because the engine counts `state.turn` per **round** (mana
implies turn 4, player counts turn 5, opponent counts turn 6 — irreconcilable) and §13.1.8's
own derivation is wrong; the final CIEDE2000 ≤5 palette gate names "three meadow/foliage
medians" although §5 lists four roles as point samples with no bound median rectangles; the
diamond gap rule is ambiguous (39 vs 42 px); §9.5's ground hexes are absent from §5; the
150 × 227 hand card at ±5° rotation exceeds the locked hand envelope; the §13.1.1 event-1
authoritative hash cannot be produced because Soul Trap resolves atomically; and the reference
paths in §1.1/§1.3 are machine-specific.

**Open corrections for the seventh pass (not yet applied):** critic-7 findings F-1 through F-15,
of which F-1 (fixture legality/turn model) and F-2 (palette-gate executability) are the P1s.
F-1 requires choosing Turn 4 with reconciled counts or an explicit unreachability annotation;
F-2 requires naming exact roles and binding median rectangles.

## Loop-Status Note for the Next Session

Critics 5, 6, and 7 all verified the document's empirical core byte-exact and rejected solely on
protocol-layer executability, with totals 88.3 → 89.1 → 88.2 — three consecutive cycles without
score convergence, where each correction pass has also introduced at least one new defect later
caught (the packet index enabling a mapping leak; the prominence definition; the §9.5 hexes;
the §13.1.8 turn derivation). The plan's stagnation clause ("three stagnant cycles … trigger an
art-direction/asset decision review; they do not permit declaring the item complete") is the
applicable remedy: before an eighth spec-only cycle, hold the decision review on whether the
remaining protocol clauses (fixture pinning, §13.1.1 instrumentation, palette-gate binding)
should be finalized against the *implemented* capture runner and fixture registry rather than
speculatively in prose — the recurring failure mode is prose contracts drifting from executable
reality, which the graybox/runner implementation would anchor. Gates must not be weakened either
way.

## Acceptance Boundary — RESOLVED BY USER DECISION (2026-07-28)

The user — the plan's sole final authority on acceptance — manually passed the art bible based on
the fifth-corrected lineage (`fe3d3b25…`, the revision with all nineteen critic-5 findings
fixed), ending the fresh-critic specification loop after seven revisions. This is recorded
exactly as what it is: a user manual pass. Agent-critic acceptance (≥93/100, no category <9.0,
wow ≥9.0, zero P1) was **not** achieved on any specification revision; critics 5–7 scored
88.3 / 89.1 / 88.2 while verifying the empirical core byte-exact each time.

The accepted artifact is the seventh-corrected revision — the direct continuation of
`fe3d3b25…` that additionally carries all fifteen critic-6 findings and all fifteen critic-7
findings, including the two P1s (fixture reachability annotation under the engine's per-round
turn model; palette-gate median-region binding):

- **Accepted hash:** `84b89838a7ea822bcad7a572e5815120d40367a632eafd9e7cc63032db7ea0e6`
- 1737 lines, 18241 words, 119048 bytes
- Scripted checks: zero missing §-references; superseded values absent; `git diff --check`
  clean; 545/545 units unaffected.

The Phase 2 art-direction-document checkbox is satisfied by this user decision. The camera
remains unlocked: the O/P graybox bake-off, golden samples, and all downstream **image**
critic gates (§13.4, §13.5) are untouched and still required — the user pass covers the
specification document only, not any rendered artifact. Production now proceeds to the
populated camera grayboxes per the plan.
