# 2026-02-09 - Tiny Fangs Multiplayer Bug Fix

## Bug: P2 starts with 2 mana instead of 1

**Symptom:** On the second player's first turn, they have 2 mana instead of 1.

**Root cause found in `server/GameEngine.js` ~line 1429-1438:**

```javascript
state.firstTurn = false;        // ← Set to false FIRST
// ...
if (!state.firstTurn) {         // ← This is now ALWAYS true
  nextPlayer.maxMana = Math.min(5, nextPlayer.maxMana + 1);
}
nextPlayer.mana = nextPlayer.maxMana;  // P2 incorrectly gets 2 mana
```

**Pattern:** Flag mutated before being read for the condition.

**Fix:** Save `firstTurn` value before mutating:

```javascript
const wasFirstTurn = state.firstTurn;  // Capture BEFORE mutation
state.firstTurn = false;

// ...later...
if (!wasFirstTurn) {                   // Use saved value
  nextPlayer.maxMana = Math.min(5, nextPlayer.maxMana + 1);
}
nextPlayer.mana = nextPlayer.maxMana;
```

**Status:** ✅ FIXED via subagents

---

## Bug 2: Phantom Wall Doesn't Damage Attacker

**Location:** `server/GameEngine.js` ~line 268

**Root cause:** Hardcoded switch case only set `negated = true`, missing the 10 damage to attacker.

**Fix:** Added damage logic matching Spike Shield pattern.

**Status:** ✅ FIXED

---

## Bug 3: Battle Log Empty in Multiplayer

**Root cause:** `render()` called BEFORE `playServerEvents()`, log entries added but never re-rendered.

**Fix:** Added `renderLog()` call after events processed.

**Status:** ✅ FIXED (included in Bug 4 fix)

---

## Bug 4: Animations Play Late (After KO Already Shown)

**Root cause (two-part):**
1. State updated to FINAL before animations play
2. Multiple stateUpdate messages pile up faster than animations complete

**Fix:** Added update queue system:
- `queueUpdate()` batches incoming updates
- `processUpdate()` waits for animations before applying final state
- Animations play on CURRENT state, then final state rendered

**Status:** ✅ FIXED

---

## Architecture Insight: Single-Player vs Multiplayer Divergence

**Single-player** (`src/effects.js`): Data-driven effects system
- Cards define effects array, engine executes them
- Adding a card = just define it

**Multiplayer server** (`server/GameEngine.js`): Hardcoded switch cases
- Each card manually translated to imperative code
- Easy to miss effects or have them drift out of sync

**Long-term fix:** Port data-driven effects system to server, or single source of truth.

#bugfix #multiplayer #tiny-fangs #architecture

---

## Architecture Unification Complete

**All 4 phases completed successfully:**

### Phase 1: Created `shared/` module
- `cards.js` - 29 creatures, 26 verses (single source of truth)
- `effects.js` - 28 pure effect primitives (no animations)
- `triggers.js` - Priority-based trigger matching
- `engine.js` - Core game operations (attack, summon, cast, etc.)
- `index.js` - 23 exports

### Phase 2: Server Migration
- Deleted duplicate `server/cards.js`
- Server now imports from `shared/`
- Phantom Wall bug pattern eliminated at source

### Phase 3: Client Migration
- `src/effects.js` wraps shared logic + animations
- `src/cards.js` re-exports from shared
- All 262 tests passing

### Phase 4: Documentation
- Updated `ARCHITECTURE.md` with multiplayer diagrams
- Added shared module architecture
- Documented "Single Source of Truth" principle

### Deployment
- Pushed to GitHub (commit 0fee3a3)
- Deployed to GitHub Pages
- Cloudflare tunnel: `wss://continually-pursue-packet-heroes.trycloudflare.com`
- Play at: https://abriskbreeze.github.io/tiny-fangs

**Result:** Add a card once in `shared/cards.js` → works in single-player AND multiplayer. No more divergent implementations.

#architecture #deployment #milestone

---

## Multiplayer Bug Fixes Round 2 (Late Night Session)

### Bugs Reported
1. P2 still getting 2 mana on first turn
2. Double damage from abilities/verses (Phantom Wall doing 20 instead of 10)
3. Vengeance not prompting player (optional triggers auto-firing)
4. Animation desync

### Fixes Applied

**Bug 1 - Mana:** Server hadn't been restarted after code fix. Restarted server (PID 45425).

**Bug 2 - Double Damage:** Investigation found it was already fixed in v0.4.62. Root cause was `processTriggers()` calling `processEffects()` twice. Fix: `needsProcessEffects` flag. Added debug logging to verify.

**Bug 3 - Vengeance Optional Triggers:** Implemented `pendingAction` flow:
- Server detects `optional: true` in triggerDef
- Returns `pendingAction` instead of auto-executing
- Client shows "Activate Vengeance?" modal with Yes/No
- New `respondOptionalTrigger` action handles player choice
- If declined, creature dies normally

### Key Insight
Server must be restarted after code changes! The mana fix was in the code but server was running old version.

### Deployment
- Commit: 24ae824
- Server: PID 45425
- Tunnel: wss://obituaries-comedy-blake-having.trycloudflare.com

#bugfix #multiplayer #optional-triggers

---

## Multiplayer Bug Fixes v0.4.64

### Bugs Fixed

1. **Ignite dealing 30 damage** → Fixed to 15 (hardcoded wrong value in server)

2. **Animations appearing center screen** → Added position caching system
   - `Anim.cacheActivePositions()` captures element centers BEFORE state update
   - `floatText()` now accepts coordinate objects or DOM elements
   - Called in `processUpdate()` before state changes

3. **Last Breath triggering at full HP** → Fixed logic
   - Changed from `owner.lp === 1` to `owner.lp <= damageAmount`
   - Now triggers only when damage would actually kill

4. **Deck/hand counts broken in multiplayer** → Use fallback
   - `deckCount ?? deck?.length ?? 0` pattern
   - In multiplayer, opponent's deck/hand arrays are empty (hidden)

### Key Pattern
Server sends `deckCount`/`handCount` for hidden info. Client must use these instead of array `.length` in multiplayer mode.

### Version: 0.4.64
- Commit: 0cca01d
- Bundle: index-CjswMJ1h.js

#bugfix #multiplayer #animations
